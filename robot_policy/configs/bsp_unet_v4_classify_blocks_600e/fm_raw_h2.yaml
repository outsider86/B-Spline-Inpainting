extends: common.yaml

data:
  prepared_path: /scratch/wangpc/B-Spline-Inpainting/robot_policy/outputs/BSP_UNET_V4_CLASSIFY_BLOCKS_600E/cache/prepared/raw
  action_representation: raw
  observation_horizon: 2

policy:
  architecture: bsp_unet_fm

wandb:
  run_suffix: v4-classify-blocks-raw-h2-batch64-600epochs
